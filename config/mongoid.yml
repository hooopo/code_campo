development:
  host: <%= ENV["host"]%>
  database: <%= ENV["database"]%>

test:
  host: <%= ENV["host"]%>
  database: <%= ENV["database"]%>

# set these environment variables on your prod server
production:
  uri: <%= ENV["MONGOHQ_URL"]%>
